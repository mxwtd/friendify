<div class="new-friend-container">
  <div class="new-friend">
  <h3 class="new-title">Edit activity</h3>
    <%= simple_form_for @activity do |t| %>
      <%= t.input :description %>
      <%= t.input :location %>
      <%= t.input :price %>
      <%= t.submit class: "center-btn more-btn"  %>
    <% end %>
  </div>
</div>
